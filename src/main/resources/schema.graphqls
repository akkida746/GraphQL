type Greeting {
  id: ID
  message: String
  name: String
}

type Query {
  getGreeting(id: ID): Greeting
  allGreetings: [Greeting]
}

type Mutation {
  newGreeting(message: String, name: String): Greeting
}